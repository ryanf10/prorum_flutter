import 'package:flutter/material.dart';
import 'package:prorum_flutter/screens/reset_password/components/body.dart';

class ResetPassordScreen extends StatelessWidget {
  const ResetPassordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
